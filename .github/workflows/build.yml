name: Build
on: 
  pull_request:
    paths-ignore: 
      - '**.md'
      - 'LICENSE'
      - 'prototype/**'
  push:
    paths-ignore: 
      - '**.md'
      - 'LICENSE'
      - 'prototype/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Nwlink
        run: |
          npm install -g nwlink
          echo "$HOME/.npm-global/bin" >> $GITHUB_PATH

      - name: Install Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: nightly    
          target: thumbv7em-none-eabihf

      - name: Install just
        uses: extractions/setup-just@v3
      
      - name: Create dummy object file
        run: printf "v 0.0 0.0 0.0\nf 1/1/1 1/1/1 1/1/1" >> obj/mesh.obj

      - name: Build app
        run: just build obj/mesh.obj

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: nw-3d-grapher
          path: target/**/nw_3d_grapher
        